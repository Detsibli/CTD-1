# Todo App

A simple Vite + React app to view todo lists

## Installation

Clone git and open a terminal in the root folder, then install via npm.

```bash
npm install
```

Then to run, npm dev run to run via local server.

```bash
npm dev run
```
